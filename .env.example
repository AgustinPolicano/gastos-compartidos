# Docker Compose Configuration

# PostgreSQL (si usás la DB local del docker-compose)
POSTGRES_USER=postgres
POSTGRES_PASSWORD=tu_password_seguro_aqui
POSTGRES_DB=gastos_compartidos

# Backend Database URL
# Opción 1: Usar PostgreSQL local de Docker
# DATABASE_URL=postgresql://postgres:tu_password_seguro_aqui@db:5432/gastos_compartidos

# Opción 2: Usar Railway (recomendado - ya tenés la DB con datos)
DATABASE_URL=postgresql://postgres:PASSWORD@HOST:PORT/railway

# CORS - Dominio de tu VPS
CORS_ORIGIN=http://tu-dominio.com

# API URL para el frontend
API_URL=http://tu-dominio.com:3000
