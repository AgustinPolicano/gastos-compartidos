# Frontend Development Dockerfile (with hot reload)
FROM node:22-alpine

WORKDIR /app

# Install dependencies
COPY package*.json ./
RUN npm install

# Copy source (will be overridden by volume mount for hot reload)
COPY . .

# Expose Angular dev server port
EXPOSE 4200

# Start dev server (host 0.0.0.0 to allow external connections)
CMD npm start -- --host 0.0.0.0
